# Selenium-POM-TestNG-AllureReport


## Steps to run tests
* run below cmd in the terminal  
  ``` mvn clean test ```

* to generate an allure report run the below cmd from the terminal

   ```allure serve allure-results```

